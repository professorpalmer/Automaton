/** Compatibility barrel. Paint should `useTokens()` from `src/theme`. */

export { CHAT_THEME, FIELD_THEME, toChatTheme, toFieldTheme, type GpuixTextTheme } from './theme/adapters'
export { DEFAULT_TOKENS, T, type Tokens } from './theme/tokens'

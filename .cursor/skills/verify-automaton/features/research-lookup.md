# Research lookup

Research job language and lookup language both dispatch analyze, never Kernel implement. Staff never dispatches.

## Sub-features

- `jobKindFor('research', …)` is `analyze` for job verbs or look-up/wiki/search phrasing
- Kernel lookup phrasing is `analyze` (not implement)
- Staff with the same text stays chat-only
- Analyze `--cwd` is the Automaton checkout, read-only

## How to get to it (user POV)

Click Research. Send `Look up why Send stays Send in Automaton staff.` The strip shows `Research · analyze · …`.

## Driving it

- `tests/domain.test.ts` jobKindFor
- `tests/session.test.ts` Research lookup + Staff never jobs
- Native walkthrough: Research lookup must paint a `Research · analyze` handle; no implement sandbox should be created.

## Gotchas

Do not send Research through the implement worktree path. Owner label is `research look up`.
